(ns proxy-router.service.default-service-test
  (:require [clojure.test :refer :all]
            [integrant.core :as ig]
            [proxy-router.service.default-service :as proxy-router]))

(deftest service-test
  ;; FIXME
  )
